stages:
  - lint
  - test
  - base
  - build
  - deploy

lint:
  image: gitlab.cloudcix.com:5005/utils/ci/lint
  stage: lint
  script:
    - flake8
  except:
    - master
    - stable

mypy:
  image: python:3.7
  stage: lint
  script:
    - pip3 install mypy
    - mypy --ignore-missing-imports builders dispatchers metrics quiescers restarters scrubbers tasks updaters celery_app.py cloudcix_token.py email_notifier.py robot.py state.py utils.py
  except:
    - master
    - stable


build_base:
  stage: base
  tags:
    - docker-build
  before_script:
    - echo "$GITLAB_KEY" > id_rsa
  script:
    - docker build -f deployment/dockerfiles/base.Dockerfile -t gitlab.cloudcix.com:5005/cloudcix/robot/base:latest --no-cache .
    - docker push gitlab.cloudcix.com:5005/cloudcix/robot/base:latest
  only:
    - master


build_alpha:
  stage: build
  tags:
    - docker-build
  script:
    - docker build -f deployment/dockerfiles/alpha.Dockerfile -t gitlab.cloudcix.com:5005/cloudcix/robot/alpha:latest --no-cache .
    - docker push gitlab.cloudcix.com:5005/cloudcix/robot/alpha:latest
  only:
  - master

deploy_alpha:
  image: gitlab.cloudcix.com:5005/cloudcix/deployment/image
  before_script:
    - cp ~/.ssh/application_framework/stage_key ~/.ssh/id_rsa
    - cp ~/.ssh/application_framework/stage_key.pub ~/.ssh/id_rsa.pub
  environment: staging
  stage: deploy
  script:
    - ansible-playbook -i deployment/hosts.yml deployment/playbook.yml
    - ansible-playbook -i deployment/worker-hosts.yml deployment/worker-playbook.yml
  only:
    - master

###############################################################################
#                         Production Build Jobs Start                         #
###############################################################################

build_toronto01:
  stage: build
  tags:
  - docker-build
  script:
  - docker build -f deployment/dockerfiles/toronto01.Dockerfile -t gitlab.cloudcix.com:5005/cloudcix/robot/toronto01:latest --no-cache .
  - docker push gitlab.cloudcix.com:5005/cloudcix/robot/toronto01:latest
  only:
  - stable

build_gov_cork:
  stage: build
  tags:
  - docker-build
  script:
  - docker build -f deployment/dockerfiles/gov_cork.Dockerfile -t gitlab.cloudcix.com:5005/cloudcix/robot/gov_cork:latest --no-cache .
  - docker push gitlab.cloudcix.com:5005/cloudcix/robot/gov_cork:latest
  only:
  - stable

build_cork01:
  stage: build
  tags:
  - docker-build
  script:
  - docker build -f deployment/dockerfiles/cork01.Dockerfile -t gitlab.cloudcix.com:5005/cloudcix/robot/cork01:latest --no-cache .
  - docker push gitlab.cloudcix.com:5005/cloudcix/robot/cork01:latest
  only:
  - stable

build_nti-cork01:
  stage: build
  tags:
  - docker-build
  script:
  - docker build -f deployment/dockerfiles/nti-cork01.Dockerfile -t gitlab.cloudcix.com:5005/cloudcix/robot/nti-cork01:latest --no-cache .
  - docker push gitlab.cloudcix.com:5005/cloudcix/robot/nti-cork01:latest
  only:
  - stable

###############################################################################
#                          Production Build Jobs End                          #
###############################################################################

deploy_production:
  image: gitlab.cloudcix.com:5005/cloudcix/deployment/image
  before_script:
    - cp ~/.ssh/application_framework/live_key ~/.ssh/id_rsa
    - cp ~/.ssh/application_framework/live_key.pub ~/.ssh/id_rsa.pub
  environment: production
  stage: deploy
  script:
    - ansible-playbook -i deployment/hosts.yml deployment/playbook.yml --extra-vars="deployment_host=production"
    # Deploy workers
    # Cork01
    - ansible-playbook -i deployment/worker-hosts.yml deployment/worker-playbook.yml --extra-vars="deployment_host=cork01"
  only:
    - stable
