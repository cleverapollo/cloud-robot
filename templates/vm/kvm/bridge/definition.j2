{# XML definition for a VLAN bridge in KVM hosts #}
<interface type="bridge" name="br{{ vlan }}">
  <start mode="onboot" />
  <bridge stp="on" delay="0.0">
    <interface type="vlan" name="cloud0.{{ vlan }}">
      <vlan tag="{{ vlan }}">
        <interface name="cloud0" />
      </vlan>
    </interface>
  </bridge>
</interface>
